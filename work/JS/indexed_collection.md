### Array and typedArray
---

+ new 
```js
let arr = [1,3,4]
let arr = Array(4)  // length为4 的空数组
let arr = new Array(1,3,4)   

let concated = arr.concat([1,3,4])  // 返回合并后的副本
    concated.join('-')   //返回 连接后的字符串
    push()  //末尾添加
    pop()     // 末尾删除
    shift()     // 开头删除
    unshift()   //开头添加
    reverse()
    sort()
    indexOf()
    map()
    filter()
    every()
    some()
```